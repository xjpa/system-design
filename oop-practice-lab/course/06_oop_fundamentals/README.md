# 06 — OOP fundamentals

Prerequisite: topics 00–05.

1. `10_bank_account.py` — encapsulate mutable state and invariants.
2. `20_value_objects.py` — model immutable descriptive values.
3. `30_issue_entity.py` — model identity and a controlled lifecycle.

Add smaller domain-model exercises here when these jumps feel too large. Useful
bridges include `LibraryBook`, `InventoryItem`, `ShoppingOrder`, and a tiny state
machine before the full issue entity.
