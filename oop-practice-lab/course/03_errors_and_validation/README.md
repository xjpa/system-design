# 03 — Errors and validation

Prerequisite: topics 00–02.

1. `10_errors_and_validation.py` — raise and catch a focused exception.

Good additions: reading tracebacks, multiple exception types, `try`/`except`/
`else`/`finally`, custom exceptions, file handling, and context managers. Teach
ordinary file cleanup before implementing a custom context manager.
