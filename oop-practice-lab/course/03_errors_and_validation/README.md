# 03 — Errors and validation

Prerequisite: topics 00–02.

1. `10_errors_and_validation.py` — raise and catch a focused exception.
2. `20_validation_patterns.py` — guard, normalize, distinguish, and translate.

Lesson 20 builds validation as a pipeline and distinguishes `TypeError` from
`ValueError`. Its port parser is a compact drill in error translation.
