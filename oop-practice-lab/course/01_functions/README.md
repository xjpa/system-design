# 01 — Functions

Prerequisite: topic 00.

1. `10_def_and_function_calls.py` — define and call a no-input function.
2. `20_parameters_and_arguments.py` — pass changing input into a function.
3. `30_return_values.py` — send a result back to the caller.

This topic should eventually grow before advanced Python is added. Useful next
lessons: more return-value drills, default and keyword arguments, decomposition,
pure functions, scope, `*args`, `**kwargs`, lambda, closures, and decorators.
