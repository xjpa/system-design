# 02 — Control flow and collections

Prerequisite: topics 00–01.

1. `10_conditions.py` — make decisions with `if`, `elif`, and `else`.
2. `20_lists_and_loops.py` — store and process ordered values.
3. `30_dictionaries.py` — model key/value data.
4. `40_problem_solving_patterns.py` — filter, transform, accumulate, and lookup.

Lesson 40 uses explicit loops so the logic stays visible. After it passes,
rewrite one filter or transform with a comprehension and compare readability.
