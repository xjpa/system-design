# 00 — Getting started

Prerequisites: none.

1. `10_how_to_use_these_lessons.py` — run a file and read a failure.
2. `20_values_and_variables.py` — values, names, assignment, and basic types.

Good additions here: terminal basics, `print`, strings, integers, booleans, and
tiny expression practice. Put functions in topic 01 instead.
