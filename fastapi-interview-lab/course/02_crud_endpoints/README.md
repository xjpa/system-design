# 02 — CRUD endpoints

Prerequisite: topics 00–01.

1. `10_create_and_read.py` — create, list, fetch, and return 404 correctly.
2. `20_update_and_delete.py` — implement partial updates and deletion.

Interview checkpoint: define what happens for a missing ID and distinguish
collection routes (`/tasks`) from member routes (`/tasks/{task_id}`). State
that the in-memory dictionary is a practice fake, not durable storage.
