# 00 — HTTP and FastAPI

Prerequisite: basic Python functions and dictionaries.

1. `10_first_endpoint.py` — create an app and implement `GET` routes.
2. `20_http_contracts.py` — choose methods, response bodies, headers, and status codes.

Interview checkpoint: describe every endpoint as method + path + inputs +
success response + error responses before writing code.
